sudo: required
services:
  - docker

before_install:
  - docker build -t hamletsargsyan/multi-client ./client
  - docker build -t hamletsargsyan/multi-nginx ./nginx
  - docker build -t hamletsargsyan/milti-server ./server
  - docker build -t hamletsargsyan/multi-worker ./worker
# Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
# Take those images and push them to docker hub 
 
  - docker push hamo066/hamletsargsyan:multi-client
  - docker push hamo066/hamletsargsyan:multi-nginx
  - docker push hamo066/hamletsargsyan:multi-server
  - docker push hamo066/hamletsargsyan:multi-worker
